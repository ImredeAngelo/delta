@custom-media --vw-medium (min-width: 500px);
@custom-media --vw-large (min-width: 768px);

:root {
	--color-delta: green;

	--max-width-l: 1100px;
	--max-width-m: 600px;
	--max-width-s: 400px;

	--highlight-main: green;
	--highlight-bk: #262626;
}