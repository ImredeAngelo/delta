import React from 'react'

/**
 * Display the profile pic of the person specified by the 'id' prop
 */
export default function ProfilePic(props) {
	const { id } = props;

	return (
		<div>TODO: Show profile of user:{id}</div>
	)
}
