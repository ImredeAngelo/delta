describe('Password hashing', () => {
    test('Generate and verify Argon2 hash', async () => {
        
    });
})

describe('User', () => {
    test('Create new user', () => {
        expect(1 + 2).toBe(3);
    });

    test('Check if user exists', () => {
        expect(1 + 2).toBe(3);
    });

    test('Delete user', () => {
        expect(1 + 2).toBe(3);
    });
})